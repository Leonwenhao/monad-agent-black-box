# Contracts

Foundry workspace for Agent Black Box contracts.

Build command:

```sh
forge build
```

Current contracts:

- `TraceRegistry.sol`: append-only trace session and event registry.
- `DemoTreasuryAction.sol`: simple executable action used by the demo runner.
