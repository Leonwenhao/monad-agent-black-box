{
  "name": "monad-agent-black-box",
  "version": "0.1.0",
  "private": true,
  "description": "Audit trail and replay layer for autonomous on-chain agents on Monad.",
  "workspaces": [
    "packages/trace-schema",
    "runner",
    "apps/web"
  ],
  "scripts": {
    "schema:build": "npm --workspace @agent-black-box/trace-schema run build",
    "schema:test": "npm --workspace @agent-black-box/trace-schema test",
    "contracts:build": "cd contracts && forge build",
    "runner:demo": "npm run schema:build && npm run contracts:build && npm --workspace runner run demo:scripted",
    "web:dev": "npm --workspace apps/web run dev",
    "web:build": "npm run schema:build && npm --workspace apps/web run build"
  },
  "engines": {
    "node": ">=20.19.0",
    "npm": ">=10.0.0"
  },
  "dependencies": {
    "viem": "^2.48.11"
  }
}
